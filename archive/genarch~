curl -s 'http://medicinedepartment.blogspot.com/' | tr "\'" '\n' | grep '^http' | grep ':' | grep 'blogspot.com' | awk -F'/' '{url=$1 "//"; for(i=3;i<=NF;i++) url = url "/" $i; print url}' | sort -u >urls.lst


